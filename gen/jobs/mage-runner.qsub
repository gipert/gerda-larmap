#!/bin/bash
#
# Author: Luigi Pertoldi - pertoldi@pd.infn.it
# Created: Sun 24 Mar 2019
#
# USAGE: qsub mage-runner.qsub <mage-macro-file>
#
#$ -P short ### queue
#$ -j y     ### have logoutput and erroroutput in the same file
#$ -cwd
#$ -o /dev/null
#$ -e /dev/null

LOGFILE=$(echo $1 | cut -f 1 -d '.').log
singularity exec ../../gerda-sw-all.img \
    /usr/bin/time -v MaGe --no-colors $1 &> $LOGFILE
